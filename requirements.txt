# Core async + HTTP
aiohttp==3.10.11
httpx==0.27.2

# HTML parsing
beautifulsoup4==4.12.3
lxml==5.3.0

# Search engine (new version, replaces duckduckgo_search)
ddgs==8.0.0

# LLM (Groq + LLaMA)
groq==0.11.0

# Data validation
pydantic==2.9.2

# Environment variables
python-dotenv==1.0.1

# Logging and utilities
tqdm==4.67.1

# CSV + JSON support (standard library, but included for compatibility)
pandas==2.2.3

# Optional but recommended
tenacity==9.0.0
